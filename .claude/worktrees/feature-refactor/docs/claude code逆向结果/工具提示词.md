  "tools": [
    {
      "name": "Agent",
      "description": "启动一个新代理来自主处理复杂的多步任务。\n\nAgent 工具会启动专门的代理（子进程），这些代理可以自主处理复杂的任务。每种代理类型都有特定的功能和可用的工具。\n\n可用的代理类型及其可访问的工具：\n- general-purpose：通用代理，用于研究复杂问题、搜索代码和执行多步任务。当你搜索关键字或文件，且不确定前几次尝试能否找到正确的匹配时，使用此代理为你执行搜索。（工具：*）\n- statusline-setup：使用此代理配置用户的 Claude Code 状态栏设置。（工具：Read, Edit）\n- Explore：专门用于探索代码库的快速代理。当你需要通过模式快速查找文件（例如 \"src/components/**/*.tsx\"）、搜索关键字代码（例如 \"API endpoints\"）或回答有关代码库的问题（例如 \"API 端点是如何工作的？\"）时使用此代理。调用此代理时，请指定所需的彻底程度：\"quick\" 表示基本搜索，\"medium\" 表示中度探索，或 \"very thorough\" 表示跨多个位置和命名约定的全面分析。（工具：除 Agent、ExitPlanMode、Edit、Write、NotebookEdit 之外的所有工具）\n- Plan：用于设计实施计划的软件架构师代理。当你需要规划任务的实施策略时使用此代理。它会返回分步计划，识别关键文件，并考虑架构权衡。（工具：除 Agent、ExitPlanMode、Edit、Write、NotebookEdit 之外的所有工具）\n- claude-code-guide：当用户询问以下问题时使用此代理（\"Claude 能...\"，\"Claude 是否...\"，\"我该如何...\"）：(1) Claude Code (CLI 工具) - 功能、钩子、斜杠命令、MCP 服务器、设置、IDE 集成、键盘快捷键；(2) Claude Agent SDK - 构建自定义代理；(3) Claude API - API 用法、工具调用[... 4335 字符已省略 ...]",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "description": {
            "description": "任务的简短（3-5 个词）描述",
            "type": "string"
          },
          "prompt": {
            "description": "代理要执行的任务",
            "type": "string"
          },
          "subagent_type": {
            "description": "用于此任务的专门代理的类型",
            "type": "string"
          },
          "model": {
            "description": "此代理的可选模型覆盖。优先于代理定义的模型前置数据。如果省略，则使用代理定义的模型，或从父级继承。",
            "type": "string",
            "enum": [
              "sonnet",
              "opus",
              "haiku"
            ]
          },
          "run_in_background": {
            "description": "设置为 true 以在后台运行此代理。完成时您将收到通知。",
            "type": "boolean"
          },
          "isolation": {
            "description": "隔离模式。\"worktree\" 创建一个临时的 git 工作树，以便代理在仓库的隔离副本上工作。",
            "type": "string",
            "enum": [
              "worktree"
            ]
          }
        },
        "required": [
          "description",
          "prompt"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "TaskOutput",
      "description": "- 从正在运行或已完成的任务（后台 shell、代理或远程会话）检索输出\n- 接受标识任务的 task_id 参数\n- 返回任务输出以及状态信息\n- 使用 block=true（默认）等待任务完成\n- 使用 block=false 对当前状态进行非阻塞检查\n- 可以使用 /tasks 命令找到任务 ID\n- 适用于所有任务类型：后台 shell、异步代理和远程会话",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "task_id": {
            "description": "要获取输出的任务 ID",
            "type": "string"
          },
          "block": {
            "description": "是否等待完成",
            "default": true,
            "type": "boolean"
          },
          "timeout": {
            "description": "最大等待时间（毫秒）",
            "default": 30000,
            "type": "number",
            "minimum": 0,
            "maximum": 600000
          }
        },
        "required": [
          "task_id",
          "block",
          "timeout"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "Bash",
      "description": "执行给定的 bash 命令并返回其输出。\n\n工作目录在命令之间保持不变，但 shell 状态不会。shell 环境从用户的配置文件（bash 或 zsh）初始化。\n\n重要提示：避免使用此工具运行 `find`、`grep`、`cat`、`head`、`tail`、`sed`、`awk` 或 `echo` 命令，除非有明确指示，或者在确认专用工具无法完成任务之后。相反，请使用相应的专用工具，因为这将为用户提供更好的体验：\n\n - 文件搜索：使用 Glob（而不是 find 或 ls）\n - 内容搜索：使用 Grep（而不是 grep 或 rg）\n - 读取文件：使用 Read（而不是 cat/head/tail）\n - 编辑文件：使用 Edit（而不是 sed/awk）\n - 写入文件：使用 Write（而不是 echo >/cat <<EOF）\n - 通信：直接输出文本（而不是 echo/printf）\n虽然 Bash 工具可以做类似的事情，但最好使用内置工具，因为它们提供更好的用户体验，并使审查工具调用和授予权限变得更容易。\n\n# 说明\n - 如果您的命令将创建新目录或文件，请首先使用此工具运行 `ls` 以验证父目录是否存在且位置正确。\n - 始终在命令中使用双引号将包含空格的文件路径括起来（例如，cd \"path with spaces/file.txt\"）\n - 尝试通过使用绝对路径并避免使用 `cd` 来在整个会话中保持当前工作目录。如果用户明确要求，您可以使用 `cd`。\n - 您可以指定一个可选的超时时间，以毫秒为单位（最长 600000 毫秒 / 10 分钟）。默认情况下，您的命令将在 120000 毫秒（2 分钟）后超时。\n - 您可以使用 `run_in_background` 参数在后台运行命令。仅当您不需要立即获得结果，并且可以在稍后命令完成时收到通知时才使用此功能。您不需要马上检查输出 - 完成后会通知您。您不需要在命令末尾使用 '&'[... 9040 字符已省略 ...]",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "command": {
            "description": "要执行的命令",
            "type": "string"
          },
          "timeout": {
            "description": "可选的超时时间（毫秒，最大 600000）",
            "type": "number"
          },
          "description": {
            "description": "用主动语态清晰、简洁地描述此命令的作用。切勿在描述中使用“复杂”或“风险”等词语 - 只需描述它的作用即可。\n\n对于简单的命令（git、npm、标准 CLI 工具），保持简短（5-10 个词）：\n- ls → \"列出当前目录中的文件\"\n- git status → \"显示工作树状态\"\n- npm install → \"安装包依赖项\"\n\n对于一眼难以解析的命令（管道命令、晦涩的标志等），添加足够的上下文来阐明其作用：\n- find . -name \"*.tmp\" -exec rm {} \\; → \"递归查找并删除所有 .tmp 文件\"\n- git reset --hard origin/main → \"放弃所有本地更改并匹配远程 main 分支\"\n- curl -s url | jq '.data[]' → \"从 URL 获取 JSON 并提取数据数组元素\"",
            "type": "string"
          },
          "run_in_background": {
            "description": "设置为 true 以在后台运行此命令。稍后使用 TaskOutput 读取输出。",
            "type": "boolean"
          },
          "dangerouslyDisableSandbox": {
            "description": "将此设置为 true 以危险地覆盖沙盒模式并在没有沙盒的情况下运行命令。",
            "type": "boolean"
          }
        },
        "required": [
          "command"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "Glob",
      "description": "- 适用于任何规模代码库的快速文件模式匹配工具\n- 支持如 \"**/*.js\" 或 \"src/**/*.ts\" 的 glob 模式\n- 返回按修改时间排序的匹配文件路径\n- 当您需要按名称模式查找文件时使用此工具\n- 当您进行需要多轮 globbing 和 grepping 的开放式搜索时，请改用 Agent 工具\n- 您可以在单个响应中调用多个工具。如果可能有用，最好推测性地并行执行多个搜索。",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "pattern": {
            "description": "用于匹配文件的 glob 模式",
            "type": "string"
          },
          "path": {
            "description": "要搜索的目录。如果未指定，将使用当前工作目录。重要：省略此字段以使用默认目录。请勿输入 \"undefined\" 或 \"null\" - 只需省略即可获得默认行为。如果提供，必须是有效的目录路径。",
            "type": "string"
          }
        },
        "required": [
          "pattern"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "Grep",
      "description": "基于 ripgrep 构建的强大搜索工具\n\n  用法：\n  - 始终使用 Grep 执行搜索任务。切勿将 `grep` 或 `rg` 作为 Bash 命令调用。Grep 工具已针对正确的权限和访问进行了优化。\n  - 支持完整的正则表达式语法（例如，\"log.*Error\"，\"function\\s+\\w+\"）\n  - 使用 glob 参数过滤文件（例如，\"*.js\"，\"**/*.tsx\"）或 type 参数（例如，\"js\"，\"py\"，\"rust\"）\n  - 输出模式：\"content\" 显示匹配行，\"files_with_matches\" 仅显示文件路径（默认），\"count\" 显示匹配计数\n  - 对于需要多轮搜索的开放式搜索，请使用 Agent 工具\n  - 模式语法：使用 ripgrep（而不是 grep）- 字面量大括号需要转义（在 Go 代码中使用 `interface\\{\\}` 查找 `interface{}`）\n  - 多行匹配：默认情况下，模式仅在单行内匹配。对于跨行模式如 `struct \\{[\\s\\S]*?field`，使用 `multiline: true`\n",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "pattern": {
            "description": "要在文件内容中搜索的正则表达式模式",
            "type": "string"
          },
          "path": {
            "description": "要搜索的文件或目录 (rg PATH)。默认为当前工作目录。",
            "type": "string"
          },
          "glob": {
            "description": "用于过滤文件的 Glob 模式（例如 \"*.js\"，\"*.{ts,tsx}\"）- 映射到 rg --glob",
            "type": "string"
          },
          "output_mode": {
            "description": "输出模式：\"content\" 显示匹配行（支持 -A/-B/-C 上下文，-n 行号，head_limit），\"files_with_matches\" 显示文件路径（支持 head_limit），\"count\" 显示匹配计数（支持 head_limit）。默认为 \"files_with_matches\"。",
            "type": "string",
            "enum": [
              "content",
              "files_with_matches",
              "count"
            ]
          },
          "-B": {
            "description": "每个匹配项之前显示的行数 (rg -B)。需要 output_mode: \"content\"，否则被忽略。",
            "type": "number"
          },
          "-A": {
            "description": "每个匹配项之后显示的行数 (rg -A)。需要 output_mode: \"content\"，否则被忽略。",
            "type": "number"
          },
          "-C": {
            "description": "上下文的别名。",
            "type": "number"
          },
          "context": {
            "description": "每个匹配项前后显示的行数 (rg -C)。需要 output_mode: \"content\"，否则被忽略。",
            "type": "number"
          },
          "-n": {
            "description": "在输出中显示行号 (rg -n)。需要 output_mode: \"content\"，否则被忽略。默认为 true。",
            "type": "boolean"
          },
          "-i": {
            "description": "不区分大小写的搜索 (rg -i)",
            "type": "boolean"
          },
          "type": {
            "description": "要搜索的文件类型 (rg --type)。常见类型：js, py, rust, go, java 等。对于标准文件类型，比使用 include 更高效。",
            "type": "string"
          },
          "head_limit": {
            "description": "将输出限制为前 N 行/条目，等同于 \"| head -N\"。适用于所有输出模式：content（限制输出行），files_with_matches（限制文件路径），count（限制计数条目）。默认为 0（无限制）。",
            "type": "number"
          },
          "offset": {
            "description": "在应用 head_limit 之前跳过前 N 行/条目，等同于 \"| tail -n +N | head -N\"。适用于所有输出模式。默认为 0。",
            "type": "number"
          },
          "multiline": {
            "description": "启用多行模式，其中 . 匹配换行符且模式可以跨行 (rg -U --multiline-dotall)。默认值：false。",
            "type": "boolean"
          }
        },
        "required": [
          "pattern"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "ExitPlanMode",
      "description": "当您处于计划模式并已完成将计划写入计划文件，准备好让用户批准时使用此工具。\n\n## 此工具如何工作\n- 您应该已经将计划写入计划模式系统消息中指定的计划文件中\n- 此工具不接受计划内容作为参数 - 它将从您写入的文件中读取计划\n- 此工具仅表示您已完成规划并准备好让用户审查和批准\n- 用户在审查时将看到您的计划文件内容\n\n## 何时使用此工具\n重要提示：仅当任务需要规划涉及编写代码的任务的实施步骤时才使用此工具。对于收集信息、搜索文件、读取文件或总体上试图了解代码库的研究任务 - 请勿使用此工具。\n\n## 使用此工具之前\n确保您的计划完整且明确：\n- 如果您对需求或方法有未解决的问题，请先使用 AskUserQuestion（在早期阶段）\n- 一旦您的计划敲定，使用此工具请求批准\n\n**重要提示：** 请勿使用 AskUserQuestion 来问“这个计划可以吗？”或“我应该继续吗？” - 这正是此工具的作用。ExitPlanMode 本身就是在请求用户批准您的计划。\n\n## 示例\n\n1. 初始任务：“在代码库中搜索并了解 vim 模式的实现” - 请勿使用退出计划模式工具，因为您不是在规划任务的实施步骤。\n2. 初始任务：“帮我实现 vim 的 yank 模式” - 在完成规划任务的实施步骤后使用退出计划模式工具。\n3. 初始任务：“添加处理用户身份验证的新功能” - 如果不确定身份验证方法（OAuth、JWT 等），请先使用 AskUserQuestion，在明确方法后再使用退出计划模式工具。\n",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "allowedPrompts": {
            "description": "实施计划所需的基于提示的权限。这些描述了操作类别而不是特定命令。",
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "tool": {
                  "description": "此提示适用的工具",
                  "type": "string",
                  "enum": [
                    "Bash"
                  ]
                },
                "prompt": {
                  "description": "操作的语义描述，例如 \"运行测试\"，\"安装依赖\"",
                  "type": "string"
                }
              },
              "required": [
                "tool",
                "prompt"
              ],
              "additionalProperties": false
            }
          }
        },
        "additionalProperties": {}
      }
    },
    {
      "name": "Read",
      "description": "从本地文件系统读取文件。您可以使用此工具直接访问任何文件。\n假设此工具能够读取机器上的所有文件。如果用户提供了文件路径，请假定该路径有效。读取不存在的文件是可以的；将会返回一个错误。\n\n用法：\n- file_path 参数必须是绝对路径，而不是相对路径\n- 默认情况下，它从文件开头最多读取 2000 行\n- 您可以选择指定行偏移量和限制（对于大文件特别有用），但建议通过不提供这些参数来读取整个文件\n- 结果使用 cat -n 格式返回，行号从 1 开始\n- 此工具允许 Claude Code 读取图像（如 PNG、JPG 等）。读取图像文件时，由于 Claude Code 是多模态 LLM，内容将以视觉方式呈现。\n- 此工具可以读取 PDF 文件 (.pdf)。对于大型 PDF（超过 10 页），您必须提供 pages 参数以读取特定的页面范围（例如，pages: \"1-5\"）。没有 pages 参数读取大型 PDF 将失败。每次请求最多 20 页。\n- 此工具可以读取 Jupyter 笔记本 (.ipynb 文件) 并返回所有单元格及其输出，结合代码、文本和可视化。\n- 此工具只能读取文件，不能读取目录。要读取目录，请通过 Bash 工具使用 ls 命令。\n- 您可以在单个响应中调用多个工具。最好推测性地并行读取多个可能有用文件的内容。\n- 您将经常被要求读取屏幕截图。如果用户提供屏幕截图路径，始终使用此工具查看路径处的文件。此工具适用于所有临时文件路径。\n- 如果您读取的文件存在但内容为空，您将收到系统提醒警告代替文件内容。",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "file_path": {
            "description": "要读取的文件的绝对路径",
            "type": "string"
          },
          "offset": {
            "description": "开始读取的行号。仅在文件太大无法一次读取时提供",
            "type": "number"
          },
          "limit": {
            "description": "要读取的行数。仅在文件太大无法一次读取时提供。",
            "type": "number"
          },
          "pages": {
            "description": "PDF 文件的页面范围（例如，\"1-5\"，\"3\"，\"10-20\"）。仅适用于 PDF 文件。每次请求最多 20 页。",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "Edit",
      "description": "在文件中执行精确的字符串替换。\n\n用法：\n- 在编辑之前，您必须在对话中至少使用一次 `Read` 工具。如果您在未读取文件的情况下尝试编辑，此工具将报错。\n- 当从 Read 工具输出中编辑文本时，请确保保留出现在行号前缀之后的精确缩进（制表符/空格）。行号前缀格式为：空格 + 行号 + 制表符。该制表符之后的所有内容都是要匹配的实际文件内容。切勿在 old_string 或 new_string 中包含行号前缀的任何部分。\n- 始终优先编辑代码库中的现有文件。除非明确要求，否则切勿编写新文件。\n- 仅在用户明确要求时使用表情符号。除非被要求，否则避免在文件中添加表情符号。\n- 如果 `old_string` 在文件中不是唯一的，编辑将失败。提供带有更多周围上下文的更大字符串使其唯一，或使用 `replace_all` 替换 `old_string` 的每个实例。\n- 使用 `replace_all` 在整个文件中替换和重命名字​​符串。例如，如果您想重命名变量，此参数很有用。",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "file_path": {
            "description": "要修改的文件的绝对路径",
            "type": "string"
          },
          "old_string": {
            "description": "要替换的文本",
            "type": "string"
          },
          "new_string": {
            "description": "要替换为的文本（必须不同于 old_string）",
            "type": "string"
          },
          "replace_all": {
            "description": "替换所有出现的 old_string（默认 false）",
            "default": false,
            "type": "boolean"
          }
        },
        "required": [
          "file_path",
          "old_string",
          "new_string"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "Write",
      "description": "将文件写入本地文件系统。\n\n用法：\n- 如果提供的路径上已有文件，此工具将覆盖现有文件。\n- 如果这是现有文件，您必须首先使用 Read 工具读取文件的内容。如果未先读取文件，此工具将失败。\n- 优先使用 Edit 工具修改现有文件 — 它仅发送差异。仅将此工具用于创建新文件或进行完全重写。\n- 除非用户明确要求，否则切勿创建文档文件 (*.md) 或 README 文件。\n- 仅在用户明确要求时使用表情符号。除非被要求，否则避免向文件写入表情符号。",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "file_path": {
            "description": "要写入的文件的绝对路径（必须是绝对路径，而不是相对路径）",
            "type": "string"
          },
          "content": {
            "description": "要写入文件的内容",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "content"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "NotebookEdit",
      "description": "完全使用新源替换 Jupyter 笔记本 (.ipynb 文件) 中特定单元格的内容。Jupyter 笔记本是结合了代码、文本和可视化的交互式文档，常用于数据分析和科学计算。notebook_path 参数必须是绝对路径，而不是相对路径。cell_number 从 0 开始索引。使用 edit_mode=insert 在 cell_number 指定的索引处添加新单元格。使用 edit_mode=delete 删除 cell_number 指定的索引处的单元格。",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "notebook_path": {
            "description": "要编辑的 Jupyter 笔记本文件的绝对路径（必须是绝对路径，而不是相对路径）",
            "type": "string"
          },
          "cell_id": {
            "description": "要编辑的单元格 ID。插入新单元格时，新单元格将插入在此 ID 的单元格之后，如果未指定则插入在开头。",
            "type": "string"
          },
          "new_source": {
            "description": "单元格的新源码",
            "type": "string"
          },
          "cell_type": {
            "description": "单元格的类型（代码或 markdown）。如果未指定，默认为当前单元格类型。如果使用 edit_mode=insert，这是必需的。",
            "type": "string",
            "enum": [
              "code",
              "markdown"
            ]
          },
          "edit_mode": {
            "description": "要进行的编辑类型（替换、插入、删除）。默认为替换。",
            "type": "string",
            "enum": [
              "replace",
              "insert",
              "delete"
            ]
          }
        },
        "required": [
          "notebook_path",
          "new_source"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "WebFetch",
      "description": "重要提示：WebFetch 对经过身份验证或私有 URL 将失败。使用此工具前，检查 URL 是否指向经过身份验证的服务（例如 Google Docs、Confluence、Jira、GitHub）。如果是，请寻找提供身份验证访问的专用 MCP 工具。\n\n- 从指定 URL 获取内容并使用 AI 模型处理它\n- 将 URL 和提示作为输入\n- 获取 URL 内容，将 HTML 转换为 markdown\n- 使用小型快速模型处理带有提示的内容\n- 返回模型关于内容的响应\n- 当需要检索和分析 Web 内容时使用此工具\n\n使用说明：\n  - 重要：如果提供了 MCP 的 web 获取工具，请优先使用该工具而不是此工具，因为它可能有较少限制。\n  - URL 必须是格式良好的有效 URL\n  - HTTP URL 将自动升级为 HTTPS\n  - 提示应描述您想从页面中提取哪些信息\n  - 此工具是只读的，不修改任何文件\n  - 如果内容非常大，结果可能会被总结\n  - 包含 15 分钟的自动清理缓存，以便在重复访问同一 URL 时提供更快的响应\n  - 当 URL 重定向到不同主机时，工具将通知您并以特殊格式提供重定向 URL。然后，您应该使用重定向 URL 发出新的 WebFetch 请求来获取内容。\n  - 对于 GitHub URL，优先通过 Bash 使用 gh CLI（例如 gh pr view, gh issue view, gh api）。\n",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "url": {
            "description": "要从中获取内容的 URL",
            "type": "string",
            "format": "uri"
          },
          "prompt": {
            "description": "在获取的内容上运行的提示",
            "type": "string"
          }
        },
        "required": [
          "url",
          "prompt"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "WebSearch",
      "description": "\n- 允许 Claude 搜索网络并使用结果来告知响应\n- 提供时事和最新数据的最新信息\n- 返回格式化为搜索结果块的搜索结果信息，包含作为 markdown 超链接的链接\n- 使用此工具访问超出 Claude 知识截止日期的信息\n- 在单个 API 调用中自动执行搜索\n\n关键要求 - 您必须遵循此要求：\n  - 回答用户问题后，您必须在响应末尾包含一个“来源：”部分\n  - 在“来源”部分中，将搜索结果中的所有相关 URL 列为 markdown 超链接：[标题](URL)\n  - 这是强制性的 - 绝不能跳过在响应中包含来源\n  - 示例格式：\n\n    [你的答案在这里]\n\n    来源：\n    - [来源标题 1](https://example.com/1)\n    - [来源标题 2](https://example.com/2)\n\n使用说明：\n  - 支持域过滤以包含或阻止特定网站\n  - 网络搜索仅在美国可用\n\n重要 - 在搜索查询中使用正确的年份：\n  - 当前月份是 2026 年 3 月。搜索最新信息、文档或当前事件时，必须使用此年份。\n  - 示例：如果用户询问“最新的 React 文档”，请使用当前年份搜索“React 文档”，而不是去年\n",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "query": {
            "description": "要使用的搜索查询",
            "type": "string",
            "minLength": 2
          },
          "allowed_domains": {
            "description": "仅包含来自这些域的搜索结果",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "blocked_domains": {
            "description": "绝不包含来自这些域的搜索结果",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "query"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "TaskStop",
      "description": "\n- 通过其 ID 停止正在运行的后台任务\n- 接受标识要停止的任务的 task_id 参数\n- 返回成功或失败状态\n- 当您需要终止长时间运行的任务时使用此工具\n",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "task_id": {
            "description": "要停止的后台任务的 ID",
            "type": "string"
          },
          "shell_id": {
            "description": "已弃用：请改用 task_id",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "AskUserQuestion",
      "description": "当您需要在执行期间向用户提问时使用此工具。这允许您：\n1. 收集用户偏好或要求\n2. 澄清模棱两可的指令\n3. 在工作时获取实施选择的决策\n4. 向用户提供关于采取什么方向的选择。\n\n使用说明：\n- 用户将始终能够选择“其他”来提供自定义文本输入\n- 使用 multiSelect: true 允许为一个问题选择多个答案\n- 如果您推荐特定选项，请将其作为列表中的第一个选项，并在标签末尾添加“(推荐)”\n\n计划模式注意：在计划模式下，在最终确定计划之前，使用此工具澄清要求或在方法之间做出选择。不要使用此工具问“我的计划准备好了吗？”或“我应该继续吗？” - 使用 ExitPlanMode 进行计划批准。重要：不要在问题中提及“计划”（例如，“您对计划有反馈吗？”，“计划看起来好吗？”），因为用户在您调用 ExitPlanMode 之前无法在 UI 中看到计划。如果您需要计划批准，请改用 ExitPlanMode。\n\n预览功能：\n在呈现用户需要视觉比较的具体构件时，在选项上使用可选的 `preview` 字段：\n- UI 布局或组件的 ASCII 模拟\n- 显示不同实现的代码片段\n- 图表变体\n- 配置示例\n\n预览内容呈现为等宽框中的 markdown。支持带有换行符的多行文本。当任何选项具有预览时，UI 切换到并排布局，左侧为垂直选项列表，右侧为预览。不要将预览用于标签和描述就足够的简单偏好问题。注意：预览仅支持单选问题（不支持 multiSelect）。\n",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "questions": {
            "description": "要问用户的问题（1-4 个问题）",
            "minItems": 1,
            "maxItems": 4,
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "question": {
                  "description": "要问用户的完整问题。应清晰、具体并以问号结尾。示例：“我们应该使用哪个库进行日期格式化？” 如果 multiSelect 为 true，请相应地表达，例如“您想启用哪些功能？”",
                  "type": "string"
                },
                "header": {
                  "description": "显示为标签的极短标识（最多 12 个字符）。示例：“身份验证方法”，“库”，“方法”。",
                  "type": "string"
                },
                "options": {
                  "description": "此问题的可用选择。必须有 2-4 个选项。每个选项应是独特、互斥的选择（除非启用了 multiSelect）。不应有“其他”选项，它将自动提供。",
                  "minItems": 2,
                  "maxItems": 4,
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "label": {
                        "description": "用户将看到并选择的此选项的显示文本。应简洁（1-5个词）并清楚地描述选择。",
                        "type": "string"
                      },
                      "description": {
                        "description": "解释此选项意味着什么或如果被选中会发生什么。用于提供有关权衡或影响的上下文。",
                        "type": "string"
                      },
                      "preview": {
                        "description": "聚焦此选项时呈现的可选预览内容。用于模拟、代码片段或帮助用户比较选项的视觉比较。有关预期内容格式，请参阅工具描述。",
                        "type": "string"
                      }
                    },
                    "required": [
                      "label",
                      "description"
                    ],
                    "additionalProperties": false
                  }
                },
                "multiSelect": {
                  "description": "设置为 true 允许用户选择多个选项而不仅仅是一个。当选择不互斥时使用。",
                  "default": false,
                  "type": "boolean"
                }
              },
              "required": [
                "question",
                "header",
                "options",
                "multiSelect"
              ],
              "additionalProperties": false
            }
          },
          "answers": {
            "description": "权限组件收集的用户答案",
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {
              "type": "string"
            }
          },
          "annotations": {
            "description": "来自用户的可选每问题注释（例如，对预览选择的注释）。按问题文本作为键。",
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {
              "type": "object",
              "properties": {
                "preview": {
                  "description": "如果问题使用了预览，则为所选选项的预览内容。",
                  "type": "string"
                },
                "notes": {
                  "description": "用户添加到其选择的自由文本注释。",
                  "type": "string"
                }
              },
              "additionalProperties": false
            }
          },
          "metadata": {
            "description": "用于跟踪和分析目的的可选元数据。不向用户显示。",
            "type": "object",
            "properties": {
              "source": {
                "description": "此问题来源的可选标识符（例如，/remember 命令的“remember”）。用于分析跟踪。",
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "required": [
          "questions"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "Skill",
      "description": "在主对话中执行技能\n\n当用户要求您执行任务时，检查是否有任何可用技能匹配。技能提供专门的功能和领域知识。\n\n当用户引用“斜杠命令”或“/<something>”（例如，“/commit”，“/review-pr”）时，他们指的是一项技能。使用此工具调用它。\n\n如何调用：\n- 使用此工具并带有技能名称和可选参数\n- 示例：\n  - `skill: \"pdf\"` - 调用 pdf 技能\n  - `skill: \"commit\", args: \"-m '修复错误'\"` - 带参数调用\n  - `skill: \"review-pr\", args: \"123\"` - 带参数调用\n  - `skill: \"ms-office-suite:pdf\"` - 使用完全限定名调用\n\n重要：\n- 可用技能在对话中的 system-reminder 消息中列出\n- 当技能匹配用户的请求时，这是一个阻止性要求：在生成关于任务的任何其他响应之前，必须优先调用相关技能工具\n- 绝不要在未实际调用此工具的情况下提及技能\n- 不要调用已经运行的技能\n- 不要将此工具用于内置 CLI 命令（如 /help，/clear 等）\n- 如果在当前对话轮中看到 <command-name> 标记，则说明技能已加载 - 直接按照说明操作，而不是再次调用此工具\n",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "skill": {
            "description": "技能名称。例如，“commit”，“review-pr”或“pdf”",
            "type": "string"
          },
          "args": {
            "description": "技能的可选参数",
            "type": "string"
          }
        },
        "required": [
          "skill"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "EnterPlanMode",
      "description": "当您即将开始一项非平凡的实施任务时主动使用此工具。在编写代码之前获得用户对您方法的认可可以防止浪费工作并确保一致性。此工具使您进入计划模式，您可以探索代码库并设计实施方法以供用户批准。\n\n## 何时使用此工具\n\n**首选使用 EnterPlanMode** 执行实施任务，除非它们很简单。当满足以下任何条件时使用它：\n\n1. **新功能实施**：添加有意义的新功能\n   - 示例：“添加注销按钮” - 它应该去哪里？点击时应该发生什么？\n   - 示例：“添加表单验证” - 什么规则？什么错误消息？\n\n2. **多种有效方法**：该任务可以通过多种不同的方式解决\n   - 示例：“向 API 添加缓存” - 可以使用 Redis、内存中、基于文件等。\n   - 示例：“提高性能” - 许多可能的优化策略\n\n3. **代码修改**：影响现有行为或结构的更改\n   - 示例：“更新登录流程” - 到底应该改变什么？\n   - 示例：“重构此组件” - 目标架构是什么？\n\n4. **架构决策**：任务需要在模式或技术之间进行选择\n   - 示例：“添加实时更新” - WebSockets 与 SSE 与 轮询\n   - 示例：“实施状态管理” - Redux 与 Context 与 自定义解决方案\n\n5. **多文件更改**：任务可能会触及超过 2-3 个文件\n   - 示例：“重构认证系统”\n   - 示例：“添加带有测试的新 API 端点”\n\n6. **需求不明确**：在了解全部范围之前需要探索\n   - 示例：“使应用更快” - 需要配置并确定瓶颈\n   - 示例：“修复结帐中的错误” - 需要调查根本原因\n\n7. **用户偏好很重要**：实施可能会合理地走向多个方向\n   - 如果[... 2171 字符已省略 ...]",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "name": "TaskCreate",
      "description": "使用此工具为当前编码会话创建结构化任务列表。这有助于您跟踪进度、组织复杂任务并向用户展示彻底性。\n它还帮助用户了解任务进度和其请求的整体进展。\n\n## 何时使用此工具\n\n在以下场景中主动使用此工具：\n\n- 复杂的多步任务 - 当任务需要 3 个或更多不同的步骤或操作时\n- 非平凡和复杂的任务 - 需要仔细规划或多个操作的任务\n- 计划模式 - 在使用计划模式时，创建任务列表来跟踪工作\n- 用户明确要求待办事项列表 - 当用户直接要求您使用待办事项列表时\n- 用户提供多个任务 - 当用户提供要完成的事情列表（编号或逗号分隔）时\n- 收到新指令后 - 立即将用户需求捕获为任务\n- 当您开始处理任务时 - 在开始工作之前将其标记为 in_progress\n- 完成任务后 - 将其标记为已完成并添加在实施期间发现的任何新跟进任务\n\n## 何时不要使用此工具\n\n在以下情况跳过使用此工具：\n- 只有一个简单明了的任务\n- 任务是微不足道的，跟踪它没有组织上的好处\n- 任务可以在不到 3 个简单的步骤中完成\n- 任务纯粹是对话性或信息性的\n\n注意，如果只有一个要做的简单任务，则不应使用此工具。在这种情况下，最好直接执行任务。\n\n## 任务字段\n\n- **subject**：祈使形式的简短、可操作的标题（例如，“修复登录流程中的认证错误”）\n- **description**：需要完成的内容的详细描述，包括上下文和接受标准\n- **activeForm**（可选）：当任务处于 in_progress 时微调器中显示的现在进行时形式（例如，“正在修复认证错误”）。如果省略，微调器将显示主题。\n\n所有[... 378 字符已省略 ...]",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "subject": {
            "description": "任务的简短标题",
            "type": "string"
          },
          "description": {
            "description": "需要完成的内容的详细描述",
            "type": "string"
          },
          "activeForm": {
            "description": "当处于 in_progress 时显示在微调器中的现在进行时形式（例如，“正在运行测试”）",
            "type": "string"
          },
          "metadata": {
            "description": "要附加到任务的任意元数据",
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {}
          }
        },
        "required": [
          "subject",
          "description"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "TaskGet",
      "description": "使用此工具通过任务列表中任务的 ID 检索任务。\n\n## 何时使用此工具\n\n- 在开始处理任务之前，当您需要完整描述和上下文时\n- 了解任务依赖关系（它阻塞了什么，什么阻塞了它）\n- 被分配任务后，以获取完整要求\n\n## 输出\n\n返回完整的任务详情：\n- **subject**：任务标题\n- **description**：详细要求和上下文\n- **status**：'pending'，'in_progress' 或 'completed'\n- **blocks**：等待此任务完成的任务\n- **blockedBy**：在此任务开始之前必须完成的任务\n\n## 提示\n\n- 获取任务后，在开始工作之前验证其 blockedBy 列表为空。\n- 使用 TaskList 以摘要形式查看所有任务。\n",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "taskId": {
            "description": "要检索的任务的 ID",
            "type": "string"
          }
        },
        "required": [
          "taskId"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "TaskUpdate",
      "description": "使用此工具更新任务列表中的任务。\n\n## 何时使用此工具\n\n**将任务标记为已解决：**\n- 当您完成任务中描述的工作时\n- 当任务不再需要或已被取代时\n- 重要：完成分配的任务时，始终将它们标记为已解决\n- 解决后，调用 TaskList 寻找您的下一个任务\n\n- 仅当您完全完成任务时才将任务标记为已完成\n- 如果遇到错误、障碍或无法完成，请将任务保持为 in_progress\n- 被阻塞时，创建一个新任务描述需要解决的问题\n- 如果出现以下情况，绝不要将任务标记为已完成：\n  - 测试失败\n  - 实施不完整\n  - 遇到未解决的错误\n  - 找不到必要的文件或依赖\n\n**删除任务：**\n- 当任务不再相关或创建错误时\n- 将状态设置为 `deleted` 永久删除任务\n\n**更新任务详情：**\n- 当需求改变或变得更清晰时\n- 当建立任务之间的依赖关系时\n\n## 您可以更新的字段\n\n- **status**：任务状态（参见下面的状态工作流）\n- **subject**：更改任务标题（祈使形式，例如，“运行测试”）\n- **description**：更改任务描述\n- **activeForm**：当 in_progress 时在微调器中显示的现在进行时形式（例如，“正在运行测试”）\n- **owner**：更改任务所有者（代理名称）\n- **metadata**：将元数据键合并到任务中（将键设置为 null 以删除它）\n- **addBlocks**：标记在此任务完成之前无法开始的任务\n- **addBlockedBy**：标记在此任务开始之前必须完成的任务\n\n## 状态工作流\n\n状态进展：`pending` → `in_progress` → `completed`\n\n使用 `deleted` 永久删除任务。\n\n## 过期数据\n\n确保在更新任务之前使用 `TaskGet` 读取任务的最新状态。\n\n## 示例\n\n开始工作时将任务标记为进行中：\n```json\n{\"taskId\": \"1\", \"status\": \"in_progres[... 385 字符已省略 ...]",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "taskId": {
            "description": "要更新的任务的 ID",
            "type": "string"
          },
          "subject": {
            "description": "任务的新主题",
            "type": "string"
          },
          "description": {
            "description": "任务的新描述",
            "type": "string"
          },
          "activeForm": {
            "description": "当 in_progress 时显示在微调器中的现在进行时形式（例如，“正在运行测试”）",
            "type": "string"
          },
          "status": {
            "description": "任务的新状态",
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "pending",
                  "in_progress",
                  "completed"
                ]
              },
              {
                "type": "string",
                "const": "deleted"
              }
            ]
          },
          "addBlocks": {
            "description": "此任务阻塞的任务 ID",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "addBlockedBy": {
            "description": "阻塞此任务的任务 ID",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "owner": {
            "description": "任务的新所有者",
            "type": "string"
          },
          "metadata": {
            "description": "合并到任务中的元数据键。将键设置为 null 以删除它。",
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {}
          }
        },
        "required": [
          "taskId"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "TaskList",
      "description": "使用此工具列出任务列表中的所有任务。\n\n## 何时使用此工具\n\n- 查看哪些任务可以处理（状态：'pending'，无所有者，未被阻塞）\n- 检查项目的整体进度\n- 查找被阻塞并需要解决依赖关系的任务\n- 完成一项任务后，检查是否有新解开阻塞的工作或认领下一个可用任务\n- **优先按 ID 顺序处理任务**（最低 ID 优先），当有多个任务可用时，因为早期任务通常为后期任务设置上下文\n\n## 输出\n\n返回每个任务的摘要：\n- **id**：任务标识符（与 TaskGet，TaskUpdate 一起使用）\n- **subject**：任务的简短描述\n- **status**：'pending'，'in_progress' 或 'completed'\n- **owner**：如果已分配则是代理 ID，如果可用则为空\n- **blockedBy**：必须首先解决的开放任务 ID 列表（具有 blockedBy 的任务在依赖关系解决之前无法被认领）\n\n将 TaskGet 与特定任务 ID 一起使用，以查看包括描述和评论在内的完整详情。\n",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "name": "EnterWorktree",
      "description": "仅当用户明确要求在工作树 (worktree) 中工作时才使用此工具。此工具创建一个隔离的 git 工作树，并将当前会话切换到其中。\n\n## 何时使用\n\n- 用户明确说“工作树”或 \"worktree\"（例如，“启动一个工作树”，“在工作树中工作”，“创建一个工作树”，“使用工作树”）\n\n## 何时不要使用\n\n- 用户要求创建分支，切换分支，或在不同分支上工作 — 请改用 git 命令\n- 用户要求修复错误或处理功能 — 除非他们具体提到工作树，否则使用常规 git 工作流\n- 除非用户明确提到“工作树”或 \"worktree\"，否则切勿使用此工具\n\n## 要求\n\n- 必须在 git 存储库中，或在 settings.json 中配置了 WorktreeCreate/WorktreeRemove 钩子\n- 不能已经在工作树中\n\n## 行为\n\n- 在 git 存储库中：在 `.claude/worktrees/` 内部创建一个新的 git 工作树，其具有基于 HEAD 的新分支\n- 在 git 存储库外部：委托给 WorktreeCreate/WorktreeRemove 钩子进行与 VCS 无关的隔离\n- 将会话的工作目录切换到新的工作树\n- 使用 ExitWorktree 在会话中途离开工作树（保留或移除）。在会话退出时，如果仍在工作树中，系统将提示用户保留或移除它\n\n## 参数\n\n- `name`（可选）：工作树的名称。如果未提供，则生成随机名称。\n",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "name": {
            "description": "工作树的可选名称（仅限字母，数字，点，下划线，破折号；最多 64 个字符）。如果未提供，则生成一个随机名称。",
            "type": "string",
            "maxLength": 64,
            "pattern": "^[a-zA-Z0-9._-]+$"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "ExitWorktree",
      "description": "退出由 EnterWorktree 创建的工作树会话，并将会话返回到原始工作目录。\n\n## 范围\n\n此工具仅在本次会话中由 EnterWorktree 创建的工作树上运行。它不会触及：\n- 您使用 `git worktree add` 手动创建的工作树\n- 来自先前会话的工作树（即使是由那时的 EnterWorktree 创建的）\n- 如果从未调用过 EnterWorktree 时您所在的目录\n\n如果在 EnterWorktree 会话之外调用，该工具是 **无操作** 的：它报告没有活动的工作树会话，不采取任何行动。文件系统状态不变。\n\n## 何时使用\n\n- 用户明确要求“退出工作树”，“离开工作树”，“返回”，或以其他方式结束工作树会话\n- 请勿主动调用此工具 — 仅在用户要求时调用\n\n## 参数\n\n- `action` (必需)：`\"keep\"` 或 `\"remove\"`\n  - `\"keep\"` — 将工作树目录和分支原封不动地留在磁盘上。如果用户想稍后回到工作中，或者有需要保留的更改，请使用此项。\n  - `\"remove\"` — 删除工作树目录及其分支。使用此选项在工作完成或被放弃时彻底退出。\n- `discard_changes` (可选，默认 false)：仅与 `action: \"remove\"` 一起有意义。如果工作树有未提交的文件或不在原始分支上的提交，该工具将拒绝删除它，除非这设置为 `true`。如果工具返回列出更改的错误，在带有 `discard_changes: true` 重新调用前请与用户确认。\n\n## 行为\n\n- 将会话的工作目录恢复到 EnterWorktree 之前的位置\n- 清除依赖于 CWD 的缓存（系统提示部分、内存文件、计划目录），以便会话状态反映原始目录\n- 如果附加到工作树的 tmux 会话：在 `remove` 时被终止，在 `keep` 时保持运行（返回其名称以便用户可以重新附加）\n- 一旦退出，可以再次调用 EnterWorktree 创建一个新的工作树\n",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "action": {
            "description": "\"keep\" 留在磁盘上的工作树和分支；\"remove\" 删除两者。",
            "type": "string",
            "enum": [
              "keep",
              "remove"
            ]
          },
          "discard_changes": {
            "description": "当 action 为 \"remove\" 且工作树具有未提交文件或未合并提交时需要设置为 true。否则该工具将拒绝操作并列出它们。",
            "type": "boolean"
          }
        },
        "required": [
          "action"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "CronCreate",
      "description": "计划在将来的时间排队执行提示。用于重复排程和一次性提醒。\n\n使用用户当地时区中的标准 5 字段 cron：分钟 小时 月中日 月 星期几。“0 9 * * *” 表示当地上午 9 点 — 不需要时区转换。\n\n## 一次性任务 (recurring: false)\n\n对于“在 X 点提醒我”或“在 <time> 时做 Y” 请求 — 触发一次然后自动删除。\n将分钟/小时/月中日/月固定为特定值：\n  “今天下午 2:30 提醒我检查部署” → cron: \"30 14 <today_dom> <today_month> *\", recurring: false\n  “明天早上，运行冒烟测试” → cron: \"57 8 <tomorrow_dom> <tomorrow_month> *\", recurring: false\n\n## 定期任务 (recurring: true, 默认)\n\n对于“每 N 分钟” / “每小时” / “工作日上午 9 点” 请求：\n  \"*/5 * * * *\" (每 5 分钟), \"0 * * * *\" (每小时), \"0 9 * * 1-5\" (当地工作日上午 9 点)\n\n## 当任务允许时，避免 :00 和 :30 的整点和半点标记\n\n每个要求“上午 9 点”的用户都会得到 `0 9`，每个要求“每小时”的用户都会得到 `0 *` — 这意味着来自全球各地的请求会在同一时刻到达 API。当用户的请求是近似的时，选择一个不是 0 或 30 的分钟：\n  “每天早上 9 点左右” → \"57 8 * * *\" 或 \"3 9 * * *\" (而不是 \"0 9 * * *\")\n  “每小时” → \"7 * * * *\" (而不是 \"0 * * * *\")\n  “大约一小时后，提醒我...” → 选择你所在的任何分钟，不要四舍五入\n\n仅当用户说出该确切时间并明确意味着它时（“准时 9:00”，“在半点”，与会议协调），才使用分钟 0 或 30。如有疑问，提早或推迟几分钟 — 用户不会注意到，而服务器机队会得到缓解。\n\n## 仅限会话\n\n任务仅存活于此 Claude 会话中 — 没有任何东西被写入磁盘，并且当 Claude 退出时作业会消失。\n\n## 运行时行为\n\n作业仅在 REPL 空闲时触发（不在查询中间）。调度器会在[... 428 字符已省略 ...]",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "cron": {
            "description": "本地时间的标准 5 字段 cron 表达式：\"M H DoM Mon DoW\" (例如 \"*/5 * * * *\" = 每 5 分钟，\"30 14 28 2 *\" = 2 月 28 日当地下午 2:30 一次)。",
            "type": "string"
          },
          "prompt": {
            "description": "在每个触发时间要排队的提示。",
            "type": "string"
          },
          "recurring": {
            "description": "true (默认) = 在每个 cron 匹配上触发，直到被删除或 7 天后自动过期。false = 在下一次匹配时触发一次，然后自动删除。将 false 用于带有固定的分钟/小时/dom/月份的“在 X 点提醒我”一次性请求。",
            "type": "boolean"
          }
        },
        "required": [
          "cron",
          "prompt"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "CronDelete",
      "description": "取消之前通过 CronCreate 安排的 cron 任务。将其从内存会话存储中删除。",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "id": {
            "description": "由 CronCreate 返回的任务 ID。",
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "CronList",
      "description": "列出在此会话中通过 CronCreate 安排的所有 cron 任务。",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "name": "mcp__ide__getDiagnostics",
      "description": "从 VS Code 获取语言诊断信息",
      "input_schema": {
        "type": "object",
        "properties": {
          "uri": {
            "type": "string",
            "description": "要获取诊断信息的可选文件 URI。如果未提供，则获取所有文件的诊断信息。"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "mcp__ide__executeCode",
      "description": "在当前笔记本文件的 Jupyter 内核中执行 Python 代码。\n    \n    所有代码将在当前的 Jupyter 内核中执行。\n    \n    除非用户明确要求，否则避免声明变量或修改内核状态。\n    \n    执行的任何代码都将在此工具的调用之间持久存在，除非内核已重新启动。",
      "input_schema": {
        "type": "object",
        "properties": {
          "code": {
            "type": "string",
            "description": "要在内核上执行的代码。"
          }
        },
        "required": [
          "code"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    }
  ]